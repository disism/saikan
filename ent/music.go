// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/disism/saikan/ent/file"
	"github.com/disism/saikan/ent/music"
)

// Music is the model entity for the Music schema.
type Music struct {
	config `json:"-"`
	// ID of the ent.
	ID uint64 `json:"id,omitempty"`
	// CreateTime holds the value of the "create_time" field.
	CreateTime time.Time `json:"create_time,omitempty"`
	// UpdateTime holds the value of the "update_time" field.
	UpdateTime time.Time `json:"update_time,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Description holds the value of the "description" field.
	Description string `json:"description,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the MusicQuery when eager-loading is set.
	Edges        MusicEdges `json:"edges"`
	file_musics  *uint64
	selectValues sql.SelectValues
}

// MusicEdges holds the relations/edges for other nodes in the graph.
type MusicEdges struct {
	// User holds the value of the user edge.
	User []*User `json:"user,omitempty"`
	// File holds the value of the file edge.
	File *File `json:"file,omitempty"`
	// Artists holds the value of the artists edge.
	Artists []*Artist `json:"artists,omitempty"`
	// Playlists holds the value of the playlists edge.
	Playlists []*Playlist `json:"playlists,omitempty"`
	// Albums holds the value of the albums edge.
	Albums []*Album `json:"albums,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [5]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading.
func (e MusicEdges) UserOrErr() ([]*User, error) {
	if e.loadedTypes[0] {
		return e.User, nil
	}
	return nil, &NotLoadedError{edge: "user"}
}

// FileOrErr returns the File value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e MusicEdges) FileOrErr() (*File, error) {
	if e.loadedTypes[1] {
		if e.File == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: file.Label}
		}
		return e.File, nil
	}
	return nil, &NotLoadedError{edge: "file"}
}

// ArtistsOrErr returns the Artists value or an error if the edge
// was not loaded in eager-loading.
func (e MusicEdges) ArtistsOrErr() ([]*Artist, error) {
	if e.loadedTypes[2] {
		return e.Artists, nil
	}
	return nil, &NotLoadedError{edge: "artists"}
}

// PlaylistsOrErr returns the Playlists value or an error if the edge
// was not loaded in eager-loading.
func (e MusicEdges) PlaylistsOrErr() ([]*Playlist, error) {
	if e.loadedTypes[3] {
		return e.Playlists, nil
	}
	return nil, &NotLoadedError{edge: "playlists"}
}

// AlbumsOrErr returns the Albums value or an error if the edge
// was not loaded in eager-loading.
func (e MusicEdges) AlbumsOrErr() ([]*Album, error) {
	if e.loadedTypes[4] {
		return e.Albums, nil
	}
	return nil, &NotLoadedError{edge: "albums"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Music) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case music.FieldID:
			values[i] = new(sql.NullInt64)
		case music.FieldName, music.FieldDescription:
			values[i] = new(sql.NullString)
		case music.FieldCreateTime, music.FieldUpdateTime:
			values[i] = new(sql.NullTime)
		case music.ForeignKeys[0]: // file_musics
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Music fields.
func (m *Music) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case music.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			m.ID = uint64(value.Int64)
		case music.FieldCreateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field create_time", values[i])
			} else if value.Valid {
				m.CreateTime = value.Time
			}
		case music.FieldUpdateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field update_time", values[i])
			} else if value.Valid {
				m.UpdateTime = value.Time
			}
		case music.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				m.Name = value.String
			}
		case music.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				m.Description = value.String
			}
		case music.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field file_musics", value)
			} else if value.Valid {
				m.file_musics = new(uint64)
				*m.file_musics = uint64(value.Int64)
			}
		default:
			m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Music.
// This includes values selected through modifiers, order, etc.
func (m *Music) Value(name string) (ent.Value, error) {
	return m.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the Music entity.
func (m *Music) QueryUser() *UserQuery {
	return NewMusicClient(m.config).QueryUser(m)
}

// QueryFile queries the "file" edge of the Music entity.
func (m *Music) QueryFile() *FileQuery {
	return NewMusicClient(m.config).QueryFile(m)
}

// QueryArtists queries the "artists" edge of the Music entity.
func (m *Music) QueryArtists() *ArtistQuery {
	return NewMusicClient(m.config).QueryArtists(m)
}

// QueryPlaylists queries the "playlists" edge of the Music entity.
func (m *Music) QueryPlaylists() *PlaylistQuery {
	return NewMusicClient(m.config).QueryPlaylists(m)
}

// QueryAlbums queries the "albums" edge of the Music entity.
func (m *Music) QueryAlbums() *AlbumQuery {
	return NewMusicClient(m.config).QueryAlbums(m)
}

// Update returns a builder for updating this Music.
// Note that you need to call Music.Unwrap() before calling this method if this Music
// was returned from a transaction, and the transaction was committed or rolled back.
func (m *Music) Update() *MusicUpdateOne {
	return NewMusicClient(m.config).UpdateOne(m)
}

// Unwrap unwraps the Music entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (m *Music) Unwrap() *Music {
	_tx, ok := m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Music is not a transactional entity")
	}
	m.config.driver = _tx.drv
	return m
}

// String implements the fmt.Stringer.
func (m *Music) String() string {
	var builder strings.Builder
	builder.WriteString("Music(")
	builder.WriteString(fmt.Sprintf("id=%v, ", m.ID))
	builder.WriteString("create_time=")
	builder.WriteString(m.CreateTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("update_time=")
	builder.WriteString(m.UpdateTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(m.Name)
	builder.WriteString(", ")
	builder.WriteString("description=")
	builder.WriteString(m.Description)
	builder.WriteByte(')')
	return builder.String()
}

// Musics is a parsable slice of Music.
type Musics []*Music
