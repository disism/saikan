// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/disism/saikan/ent/vodeo"
)

// Vodeo is the model entity for the Vodeo schema.
type Vodeo struct {
	config
	// ID of the ent.
	ID           int `json:"id,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Vodeo) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case vodeo.FieldID:
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Vodeo fields.
func (v *Vodeo) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case vodeo.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			v.ID = int(value.Int64)
		default:
			v.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Vodeo.
// This includes values selected through modifiers, order, etc.
func (v *Vodeo) Value(name string) (ent.Value, error) {
	return v.selectValues.Get(name)
}

// Update returns a builder for updating this Vodeo.
// Note that you need to call Vodeo.Unwrap() before calling this method if this Vodeo
// was returned from a transaction, and the transaction was committed or rolled back.
func (v *Vodeo) Update() *VodeoUpdateOne {
	return NewVodeoClient(v.config).UpdateOne(v)
}

// Unwrap unwraps the Vodeo entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (v *Vodeo) Unwrap() *Vodeo {
	_tx, ok := v.config.driver.(*txDriver)
	if !ok {
		panic("ent: Vodeo is not a transactional entity")
	}
	v.config.driver = _tx.drv
	return v
}

// String implements the fmt.Stringer.
func (v *Vodeo) String() string {
	var builder strings.Builder
	builder.WriteString("Vodeo(")
	builder.WriteString(fmt.Sprintf("id=%v", v.ID))
	builder.WriteByte(')')
	return builder.String()
}

// Vodeos is a parsable slice of Vodeo.
type Vodeos []*Vodeo
